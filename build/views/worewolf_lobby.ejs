<!DOCTYPE html>
<html>
  <head>
    <%- include("head", {title: "人狼ロビー"}) %>
    <link rel='stylesheet' href='./stylesheets/lobby.css' />
  </head>
  <body>

    <div id="container">
<%- include("header_login", {title: "ロビー"}) %>
    
    <div id="table_wrap">

    <% if(result.length == 0) {%>
        現在部屋はありません。<br>
        <a href="./makeroom">新しく部屋を作成</a>
    <% } else { %>
        <% for(var room of result) { %>
          <div class="village">
            <div class="village_title <%= room.state %>">
              <%= room.vno %>番 <a href="./worewolf/<%= room.vno %>"><%= room.name %></a>
            </div>
            <div class="village_detail">
              <%= room.pr %><br>
              昼:<%= room.time.day %>秒 投票:<%= room.time.vote %>秒 夜:<%= room.time.night %>秒 能力:<%= room.time.ability %>秒<br>
              定員:<%= room.capacity %>人<br>
              GM:<%= room.GMid %>
            </div>
          </div>
        <% } %>
        <a href="./makeroom">新しく部屋を作成</a>
    <% } %>
    </div>
	</div>
  </body>
</html>
